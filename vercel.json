{
  "version": 2,
  "installCommand": "npm install -g pnpm && pnpm install",
  "buildCommand": "pnpm --filter @workspace/frontend run build && node -e \"const fs=require('fs');if(fs.existsSync('frontend/dist')){if(fs.existsSync('dist'))fs.rmSync('dist',{recursive:true,force:true});fs.cpSync('frontend/dist','dist',{recursive:true});console.log('Copied frontend/dist to dist');}\"",
  "outputDirectory": "dist",
  "env": {
    "PNPM_HOME": ".pnpm",
    "NODE_ENV": "production"
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "https://medigo-api.onrender.com/api/$1"
    },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
